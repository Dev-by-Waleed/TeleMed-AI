export const metadata = {
  title: "Patient Portal | TeleMed AI",
  description: "Your TeleMed AI patient portal.",
};

export default function PatientLayout({ children }) {
  return <>{children}</>;
}
